#!kivy 1.5
<Loop>:
    play: play
    record: record
    trig: trig
    once: once
    twice: twice
    halfth: halfth
    overdub: overdub
    multiply: multiply
    pause: pause
    mute: mute
    reverse: reverse
    dry: dry
    wet: wet
    rate: rate
    undo: undo
    redo: redo
    sync: sync
    play_sync: play_sync

        
    orientation: 'horizontal'
    size_hint: 1, 1

    BoxLayout:
        size_hint: 1, 1
        orientation: 'vertical'
        Toggle:
            id: play
            text: 'Play'
            main_color: root.color
            group: 'sl_transport_' + root.name
            name: 'Play'
            osc_name: "SooperLooper " + root.name
        Toggle:
            id: record
            text: 'Record'
            name: 'Record'
            osc_name: "SooperLooper " + root.name
            main_color: root.color
            group: 'sl_transport_' + root.name

    BoxLayout:
        orientation: 'vertical'
        size_hint: None, 1
        width: root.width * 2 / 3

        GridLayout:
            cols: 4
            rows: 2
            size_hint: 1, 1

            Toggle:
                id: overdub
                text: 'Overdub'
                main_color: root.color
                group: 'sl_overdub_' + root.name
                name: 'Overdub'
                osc_name: "SooperLooper " + root.name
            Toggle:
                id: multiply
                text: 'Multiply'
                main_color: root.color
                group: 'sl_overdub_' + root.name
                name: 'Multiply'
                osc_name: "SooperLooper " + root.name
            Toggle:
                id: reverse
                text: 'Reverse'
                main_color: root.color
                name: 'Reverse'
                osc_name: "SooperLooper " + root.name
            Push:
                id: undo
                text: 'Undo'
                main_color: root.color
                name: 'Undo'
                osc_name: "SooperLooper " + root.name

            Push:
                id: trig
                text: 'Trig'
                main_color: root.color
                group: 'sl_overdub_' + root.name
                name: 'Trig'
                osc_name: "SooperLooper " + root.name
            Toggle:
                id: pause
                text: 'Pause'
                main_color: root.color
                group: 'sl_transport_' + root.name
                name: 'Pause'
                osc_name: "SooperLooper " + root.name
            Toggle:
                id: mute
                text: 'Mute'
                main_color: root.color
                name: 'Mute'
                osc_name: "SooperLooper " + root.name
            Push:
                id: redo
                text: 'Redo'
                main_color: root.color
                name: 'Redo'
                osc_name: "SooperLooper " + root.name

        BoxLayout:
            size_hint: 1, 1
            orientation: 'vertical'

            ValueFader:
                id: wet
                name: 'Wet'
                color: root.color
                orientation: 'horizontal'
                osc_name: "SooperLooper " + root.name
                max: 1
                step: 0.001
            ValueFader:
                id: dry
                name: 'Dry'
                color: root.color
                orientation: 'horizontal'
                osc_name: "SooperLooper " + root.name
                max: 1
                step: 0.001


    ValueFader:
        id: rate
        name: 'Rate'
        color: root.color
        orientation: 'vertical'
        osc_name: "SooperLooper " + root.name
        max: 4
        min: 0.001
        step: 0.001

    BoxLayout:
        orientation: 'vertical'
        Toggle:
            id: play_sync
            text: 'Play Sync'
            main_color: root.color
            name: "Play Sync"
            osc_name: "SooperLooper " + root.name
        Toggle:
            id: sync
            text: 'Sync'
            main_color: root.color
            name: "Sync"
            osc_name: "SooperLooper " + root.name
        Toggle:
            id: halfth
            text: 'x1/2'
            main_color: root.color
            group: 'sl_rate_' + root.name
            name: "xdemi"
            osc_name: "SooperLooper " + root.name
        Toggle:
            id: once
            text: 'x1'
            main_color: root.color
            group: 'sl_rate_' + root.name
            name: "x1"
            osc_name: "SooperLooper " + root.name
        Toggle:
            id: twice
            text: 'x2'
            main_color: root.color
            group: 'sl_rate_' + root.name
            name: "x2"
            osc_name: "SooperLooper " + root.name

            
            
